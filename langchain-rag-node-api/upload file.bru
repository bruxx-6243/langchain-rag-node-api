meta {
  name: upload file
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/upload-file
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file()
}

script:post-response {
  const filename = res.getBody().filename;
  
  bru.setEnvVar("filename", filename);
  
}
