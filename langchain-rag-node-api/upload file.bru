meta {
  name: upload file
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/upload-file
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file(C:\Users\LENOVO\Downloads\obama_yes_we_can_summary.txt)
}

script:post-response {
  const filename = res.getBody().filename;
  
  bru.setEnvVar("filename", filename);
  
}
