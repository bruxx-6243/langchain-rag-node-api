meta {
  name: ask question
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/ask-question
  body: json
  auth: inherit
}

body:json {
  {
    "question": "C'est qui Barack Obama?",
    "filename": "{{filename}}"
  }
}
